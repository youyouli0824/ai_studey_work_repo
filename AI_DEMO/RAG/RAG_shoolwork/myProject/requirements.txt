# ============================================================
#  企业人事制度知识库问答系统 依赖清单
#  已在本机 Python 3.14 环境验证安装通过
#  安装：python -m pip install -r requirements.txt
# ============================================================

# ---- RAG 框架 / 向量存储（LlamaIndex 内置向量存储） ----
llama-index-core>=0.14.0,<0.15
llama-index-vector-stores-chroma>=0.5.0,<0.6
chromadb>=1.5.0,<2.0

# ---- Embedding / Rerank（本地 BGE 模型底层） ----
sentence-transformers>=3.0
torch>=2.0
transformers>=4.40

# ---- 大模型调用（DeepSeek / 在线 Embedding，OpenAI 兼容） ----
openai>=1.0

# ---- 文档加载 ----
python-docx>=1.1

# ---- 其他工具 ----
python-dotenv>=1.0
numpy>=1.24
